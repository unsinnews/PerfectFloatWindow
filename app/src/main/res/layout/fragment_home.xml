<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/rootLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:clipChildren="false"
    android:clipToPadding="false"
    android:background="#FFFFFF">

    <!-- Spacer top - smaller -->
    <View
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="0.6"/>

    <!-- Header Area - more compact -->
    <LinearLayout
        android:id="@+id/headerLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingStart="24dp"
        android:paddingEnd="24dp"
        android:gravity="center_horizontal">

        <!-- App Icon - smaller -->
        <FrameLayout
            android:id="@+id/iconContainer"
            android:layout_width="60dp"
            android:layout_height="60dp"
            android:background="@drawable/float_bg_light_green_gray">

            <ImageView
                android:id="@+id/ivAppIcon"
                android:layout_width="30dp"
                android:layout_height="30dp"
                android:layout_gravity="center"
                android:src="@drawable/ic_float_ai"/>

        </FrameLayout>

        <TextView
            android:id="@+id/tvAppName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:text="AI 解题助手"
            android:textSize="24sp"
            android:textColor="#202123"
            android:textStyle="bold"/>

        <TextView
            android:id="@+id/tvAppDesc"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:text="截图识别，智能解答"
            android:textSize="14sp"
            android:textColor="#6E6E80"/>

    </LinearLayout>

    <!-- Spacer between header and content - larger -->
    <View
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"/>

    <!-- Main Content -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingStart="20dp"
        android:paddingEnd="20dp"
        android:clipChildren="false"
        android:clipToPadding="false">

        <!-- Float Window Toggle Card - with elevation for emphasis -->
        <com.google.android.material.card.MaterialCardView
            android:id="@+id/cardFloatToggle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:cardCornerRadius="16dp"
            app:cardElevation="6dp"
            app:cardBackgroundColor="#F7F7F8"
            app:strokeWidth="0dp"
            xmlns:app="http://schemas.android.com/apk/res-auto">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical"
                android:padding="24dp">

                <ImageView
                    android:id="@+id/ivFloatIcon"
                    android:layout_width="48dp"
                    android:layout_height="48dp"
                    android:padding="10dp"
                    android:background="@drawable/bg_icon_circle"
                    android:src="@drawable/ic_float_ai"
                    android:tint="#10A37F"/>

                <LinearLayout
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:layout_marginStart="16dp"
                    android:orientation="vertical">

                    <TextView
                        android:id="@+id/tvFloatTitle"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="悬浮球"
                        android:textSize="18sp"
                        android:textColor="#202123"
                        android:textStyle="bold"/>

                    <TextView
                        android:id="@+id/tvFloatStatus"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_marginTop="4dp"
                        android:text="点击开启后，悬浮球将显示在屏幕上"
                        android:textSize="13sp"
                        android:textColor="#6E6E80"/>

                </LinearLayout>

                <androidx.appcompat.widget.SwitchCompat
                    android:id="@+id/switchFloat"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"/>

            </LinearLayout>

        </com.google.android.material.card.MaterialCardView>

        <!-- Usage Tip - lighter weight -->
        <LinearLayout
            android:id="@+id/tipLayout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="20dp"
            android:orientation="vertical"
            android:background="@drawable/bg_tip_light_green_gray"
            android:padding="16dp">

            <TextView
                android:id="@+id/tvTipTitle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="使用提示"
                android:textSize="13sp"
                android:textColor="#10A37F"
                android:textStyle="bold"/>

            <TextView
                android:id="@+id/tvTipContent"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="6dp"
                android:text="1. 开启悬浮球后，切换到题目页面\n2. 点击悬浮球截取屏幕\n3. AI 将自动识别并解答题目"
                android:textSize="12sp"
                android:textColor="#6E6E80"
                android:lineSpacingMultiplier="1.3"/>

        </LinearLayout>

    </LinearLayout>

    <!-- Spacer bottom -->
    <View
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1.2"/>

</LinearLayout>
